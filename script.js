// Função para scroll suave entre as seções
function scrollToSection(sectionId) {
    document.querySelector(sectionId).scrollIntoView({ behavior: 'smooth' });
}
// Função para rolar suavemente até uma seção

